{
  "name": "ini-lint",
  "version": "0.1.0",
  "description": "A linter for INI files that reports findings with line and column numbers.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "ini-lint": "dist/cli.js"
  },
  "main": "dist/lint.js",
  "types": "dist/lint.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "engines": {
    "node": ">=18"
  }
}
